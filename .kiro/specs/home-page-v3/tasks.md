# Implementation Plan: home-page-v3

- [ ] 1. Foundation: 型・設定・共有ロジック
- [x] 1.1 ウィジェットの型定義・メタデータディスクリプタ・有効レイアウト解決関数を作る
  - 7ウィジェットのキー、ディスクリプタの型（キー・見出しキー・既定の表示可否・既定の順序・全幅か否か。コンポーネントを差し込むフィールドを型として持つが、この時点では既存4ウィジェットのみ束ね、新3ウィジェットの差し込みは統合フェーズで行う）、サイト共通設定と個人別設定の形（キーごとの表示可否・順序の部分指定）を定義する
  - 「ディスクリプタ既定 → サイト共通設定 → 個人別設定」の順で、キーごとに最初に指定された表示可否・順序を採用し、表示対象のみを順序どおりに並べた結果を返す純粋関数を実装する
  - 不正なサイト共通設定（壊れたデータ・未知のキー・型不一致）は該当部分を無視してディスクリプタ既定にフォールバックする
  - Classroom の既定取得元パスと、既定ウィジェットレイアウトの定数を定義する
  - 完了状態: 解決関数に「サイト共通で非表示・個人別で表示」を与えると当該キーが結果に含まれ、壊れた設定を与えても既定順で全キーが返ることを、フィクスチャを使った単体テストで確認できる
  - _Requirements: 1.1, 1.3, 1.4, 5.4, 5.5, 6.3, 6.5_
- [x] 1.2 サイト共通のウィジェット設定キーを追加する
  - 「ウィジェットの表示可否・順序のマップ（既定は空）」「ピン留めページの一覧（対象パスと任意の表示名、既定は空配列）」「Classroom の取得元パス（既定は未設定）」の3つの設定を、管理画面専用（環境変数フォールバックなし）として定義する
  - 完了状態: 設定管理経由でこれら3キーを読み書きでき、初期状態ではそれぞれ空マップ・空配列・未設定が返ることを確認できる
  - _Requirements: 3.1, 3.2, 5.1, 5.2_
  - _Depends: 1.1_
- [ ] 1.3 (P) 個人別のウィジェット設定を1利用者UI設定に追加する
  - 1利用者1ドキュメントの UI 設定に「ウィジェットのキーごとの表示可否・順序の部分指定」フィールドを追加する（スキーマ・型・更新許可リストの整合を取る）
  - 完了状態: 当該フィールドが保存・取得でき、未設定の利用者では空として扱われることを確認できる
  - _Requirements: 6.2_
  - _Depends: 1.1_
  - _Boundary: UserUISettings_
- [ ] 1.4 (P) 部イベントページ本文のパースをクライアントセーフな共有ユーティリティに切り出す
  - 既存の出欠リマインダー機能内にあるイベント行パース処理とイベントページのパスを、mongoose 非依存の共有ユーティリティとして抽出し、既存機能側もそれを参照するよう変更して単一ソース化する
  - ページ本文末尾にナビゲーションフッターが付いた文字列でも日付行のみを抽出する
  - 完了状態: 抽出したユーティリティが日付行の配列を返し、既存の出欠リマインダー機能のテストが引き続き通ることを確認できる
  - _Requirements: 4.1_
  - _Boundary: parse-club-events_
- [ ] 1.5 (P) パス配下の最近更新ページ取得・複数パス解決のサーバークエリを追加する
  - ページ一覧取得サービスに「指定パス前方一致・更新日時降順・件数制限・viewer 権限フィルタ済み」の取得メソッドと、「指定パス配列を viewer 権限でフィルタし入力順を保って解決する」メソッドを追加する
  - パス前方一致の正規表現は非ASCII文字を含むパスでも壊れないエスケープ関数を用いる
  - 既存の viewer 権限フィルタ機構を経由し、パス一致だけで権限判定しない。ゴミ箱配下・空ページは対象外
  - 完了状態: 閲覧権限のないページ・存在しないパスが結果から除外され、前方一致で日本語を含むパスでもエラーにならないことをテストで確認できる
  - _Requirements: 2.1, 2.2, 3.3, 3.5_
  - _Boundary: page-listing service_

- [ ] 2. Core: サーバーAPIとサイト共通設定の管理画面
- [ ] 2.1 パス配下最近ページ・複数パス解決のAPIエンドポイントを追加する
  - ログイン利用者のみアクセス可能な「取得元パス前方一致の最近ページ一覧」エンドポイントと、「複数パスを権限フィルタして解決する」エンドポイントを追加する
  - 取得元パス未指定時は Classroom の既定取得元パスを用いる。パスの形式を軽く検証する
  - 完了状態: 未ログイン時は認可エラー、ログイン時は viewer 権限でフィルタされた結果が返り、複数パス解決は存在しない/権限の無いパスを欠落させて返すことを確認できる
  - _Requirements: 2.1, 3.3, 3.5_
  - _Depends: 1.5_
- [ ] 2.2 (P) サイト共通ウィジェット設定の更新APIを追加する
  - 管理者のみが、ウィジェット表示可否・順序のマップ／ピン留めページ一覧／Classroom 取得元パスの3値を1リクエストでまとめて更新できるエンドポイントを追加する。更新は既存の監査ログ機構に記録される
  - 既存のカスタマイズ設定の取得API（集約）に、これら3キーの現在値を含める
  - 完了状態: 管理者以外の更新要求が拒否され、管理者からの更新で3キーが永続化され、監査アクティビティが発火することを確認できる
  - _Requirements: 5.1, 5.2, 5.3_
  - _Depends: 1.2_
  - _Boundary: customize-setting route_
- [ ] 2.3 (P) 個人別ウィジェット設定の書き込み経路を拡張する
  - 既存の1利用者UI設定の更新エンドポイントが、ウィジェットの個人別設定フィールドを受け付けて永続化するようにする
  - 完了状態: 利用者が自分の設定を更新でき、他の利用者の設定に影響しないことを確認できる
  - _Requirements: 6.2_
  - _Depends: 1.3_
  - _Boundary: user-ui-settings route_
- [ ] 2.4 (P) サイト共通設定をホーム画面の初回描画に渡すサーバー処理を追加する
  - ホームページのサーバーサイド処理に、3つのサイト共通設定を読み取ってページの props に含める処理を追加する（DBクエリなし、設定読み取りのみ）
  - 個人別設定は既にハイドレートされている1利用者UI設定に相乗りするため、この処理には含めない
  - 完了状態: 設定が未保存でも既定値（空マップ・空配列・未設定）が props として渡り、ホームページがサーバー側で描画できることを確認できる
  - _Requirements: 5.3, 5.5_
  - _Depends: 1.2_
  - _Boundary: home server, pages/home_
- [ ] 2.5 管理画面のクライアント状態にサイト共通設定の取得・更新を追加する
  - 既存のカスタマイズ設定コンテナに、3つのサイト共通設定の状態と、まとめて更新するメソッドを追加する
  - 完了状態: 管理画面側から3設定の現在値取得と更新呼び出しができることを確認できる
  - _Requirements: 5.1, 5.2_
  - _Depends: 2.2_
  - _Boundary: AdminCustomizeContainer_
- [ ] 2.6 サイト共通ウィジェット設定の編集フォームを管理画面に追加する
  - 7ウィジェットを一覧表示し、各行に上下移動と表示/非表示の操作、展開すると個別オプション（ピン留めページの一覧エディタ、Classroom の取得元パス、リスト系ウィジェットの表示件数）を編集できるフォームを実装し、管理画面のカスタマイズ設定に組み込む
  - 編集内容はフォームのローカル状態で保持し、保存操作で3値をまとめて更新する
  - ピン留めページのパスは非空・スラッシュ始まりを軽く検証する（存在確認は表示時のパス解決が担う）
  - 完了状態: 管理者がウィジェットの順序変更・非表示・ピン留めページ追加・Classroom パス設定を行って保存でき、再度開いたときに保存内容が反映されていることを確認できる
  - _Requirements: 5.1, 5.2_
  - _Depends: 2.5_
  - _Boundary: CustomizeHomeWidgetsSetting_

- [ ] 3. Core: 新ウィジェットとデータ取得
- [ ] 3.1 (P) パス配下最近ページ・複数パス解決のクライアント取得の仕組みを追加する
  - 2.1 のエンドポイントを呼び出し、結果をキャッシュ・再利用できる形で提供する2つの取得手段を追加する
  - 完了状態: ホーム画面のコンポーネントから、取得元パス配下の最近ページ一覧と、指定パス群の解決結果を取得できることを確認できる
  - _Requirements: 2.1, 3.3_
  - _Depends: 2.1_
  - _Boundary: stores/page-listing_
- [ ] 3.2 (P) 今後のイベント・出欠状態のクライアント取得の仕組みを追加する
  - 生 markdown 取得エンドポイントで部イベントページ本文を取得し、共有パースユーティリティで解析して開催日の近い順に整形する取得手段と、利用者本人の出欠回答状態（未回答か否か）を読む取得手段を追加する
  - 完了状態: ホーム画面のコンポーネントから、今後のイベント一覧と自分の未回答状態を取得できることを確認できる
  - _Requirements: 4.1, 4.3_
  - _Depends: 1.4_
  - _Boundary: stores/home-feed_
- [ ] 3.3 (P) Classroom最新投稿ウィジェットを実装する
  - サイト共通の取得元パス（未設定なら既定パス）配下の Classroom 由来ページを、更新日時の新しい順に一定件数、権限フィルタ済みで一覧表示する
  - 項目クリックで該当ページへ遷移する。0件時は空状態メッセージを表示する
  - 取得元パスは props として受け取る（実際の受け渡し配線は統合フェーズ）。見出しと空状態文言を、このウィジェット専用の i18n キー名前空間で用意する
  - 完了状態: 取得元パス配下にページがあるときに一覧が表示され、無いときに空状態が表示され、権限の無いページが含まれないことを確認できる
  - _Requirements: 2.1, 2.2, 2.3, 2.4, 2.5_
  - _Depends: 3.1_
  - _Boundary: ClassroomPostsWidget_
- [ ] 3.4 (P) ピン留めページウィジェットを実装する
  - サイト共通設定のピン留めページ一覧を、管理者指定の順序で解決して表示する。表示名は指定があればそれを、無ければページタイトルを用いる
  - 存在しない/権限の無いパスは一覧に表示しない。1件も設定が無い場合は管理者にのみ設定を促す案内を表示し、管理者以外にはこのウィジェットを描画しない
  - ピン留め一覧は props として受け取る（実際の受け渡し配線は統合フェーズ）。項目クリックで該当ページへ遷移する。見出し・案内・空状態文言を、このウィジェット専用の i18n キー名前空間で用意する
  - 完了状態: 管理者が設定したピン留めページが順序どおり表示され、未設定時は管理者にのみ案内が出て一般利用者には出ないことを確認できる
  - _Requirements: 3.1, 3.3, 3.4, 3.5_
  - _Depends: 3.1_
  - _Boundary: PinnedPagesWidget_
- [ ] 3.5 (P) お知らせ予定フィードウィジェットを実装する
  - 「今後のイベント」「未回答の出欠確認（あるときのみ）」「直近の自分宛の通知」の3セクションを1つのウィジェットにまとめて表示する
  - 各セクションのデータ取得は独立させ、1セクションの失敗が他セクションの表示を妨げないようにする
  - 各項目クリックで対応するページまたは回答画面へ遷移する。3セクションすべて表示対象が無ければウィジェット全体の空状態を表示する
  - 直近の通知は既存の通知取得の仕組みをそのまま利用する。出欠案内は既存モーダルと二重に見えないよう控えめな1行にとどめ、モーダル側の抑制ロジックには関与しない
  - 見出し・各セクション見出し・空状態文言を、このウィジェット専用の i18n キー名前空間で用意する
  - 完了状態: 今後のイベント・未回答出欠・直近通知がそれぞれ表示され、1セクションのデータ取得が失敗しても他が表示され、すべて空なら空状態が出ることを確認できる
  - _Requirements: 4.1, 4.2, 4.3, 4.4, 4.5_
  - _Depends: 3.2_
  - _Boundary: HomeFeedWidget_

- [ ] 4. Integration: ホーム画面への統合
- [ ] 4.1 ウィジェットエリアを解決済みレイアウト駆動に変更し、7ウィジェットをディスクリプタに束ねる
  - 既存4ウィジェットと新3ウィジェット（Classroom最新投稿・ピン留めページ・お知らせ予定フィード）を、1.1 で用意したディスクリプタに束ねる（各ウィジェットへ渡す props＝取得元パス・ピン留め一覧などをこの層で配線する）
  - コード固定配列の描画をやめ、「ディスクリプタ・サイト共通設定・個人別設定」から解決した有効レイアウトを、順序どおり・表示対象のみ描画するようにする
  - 各ウィジェットの個別エラー境界（v2 踏襲）を維持し、1つの失敗が他やページ全体に波及しないようにする
  - 全幅ウィジェットは1列、その他は画面幅に応じて折り返すグリッドで表示する。表示対象が0個でもレイアウトを崩さない
  - 完了状態: 管理者/個人の設定に応じて表示ウィジェットと順序が変わり、いずれか1ウィジェットを意図的に失敗させても他が表示され続け、表示0個でも崩れないことを確認できる
  - _Requirements: 1.1, 1.2, 1.3, 5.3, 6.3, 7.2, 7.3, 7.4_
  - _Depends: 1.1, 2.4, 3.3, 3.4, 3.5_
  - _Boundary: HomeWidgets_
- [ ] 4.2 ホーム画面本体にヒーロー領域とサイト共通設定の受け渡しを統合する
  - ページ上部に wiki 名と簡単な説明を示すヒーロー領域を追加する
  - サーバーから渡るサイト共通設定と、ハイドレート済みの1利用者UI設定を、ウィジェットエリアと各ウィジェットへ配る
  - ゲスト（未ログイン）にはウィジェットエリアを表示せず、お知らせと動作環境の要件表のみを表示する既存動作を維持する
  - 完了状態: ログイン利用者にはヒーロー＋ウィジェット＋下部セクション、ゲストにはヒーロー＋下部セクションのみが表示されることを確認できる
  - _Requirements: 7.1, 8.1, 8.2_
  - _Depends: 4.1, 2.4_
  - _Boundary: HomeContent_
- [ ] 4.3 利用者による個人別カスタマイズUIを統合する
  - ホーム画面上で、各ウィジェットカードに上下移動と表示/非表示の操作を重ねられるカスタマイズモードと、「初期状態に戻す」操作を提供する
  - 変更は有効レイアウトのマップ全体をローカル状態で保持し、保存時に個人別設定として当該利用者に永続化する。「初期状態に戻す」で個人別設定を空にする
  - カスタマイズモード中はデータ再取得中でもカードの枠（見出し）を保ってレイアウトを維持する
  - 完了状態: 利用者が自分のホームでウィジェットを並べ替え・非表示にして保存し、リロード後も維持され、「初期状態に戻す」でサイト共通設定に従った状態に戻ることを確認できる
  - _Requirements: 6.1, 6.2, 6.4_
  - _Depends: 4.2, 2.3_
  - _Boundary: HomeWidgetCustomizePanel_
- [ ] 4.4 (P) `/home` を予約パス化する
  - ページ作成不可パスの一覧に `/home` を追加し、利用者が `/home` に wiki ページを作成できないようにする
  - 起動時に `/home` にページが存在する場合は警告ログを出す処理を追加する（自動削除・自動リネームはしない）
  - 完了状態: `/home` へのページ作成が拒否され、既存の `/home` ページがある環境では起動ログに警告が出ることを確認できる
  - _Requirements: 9.1_
  - _Boundary: page-path-utils, migration_

- [ ] 5. Validation
- [ ] 5.1 (P) 有効レイアウト解決の単体テストを追加する
  - 既定のみ／サイト共通で順序・非表示を上書き／個人別が優先／個人別で「サイト共通が非表示にしたウィジェット」を表示に戻す／壊れたサイト共通設定は既定にフォールバック、の各パターンを検証する
  - 完了状態: 上記パターンそれぞれの検証が揃い、すべて成功する
  - _Requirements: 5.3, 5.4, 5.5, 6.3, 6.5_
  - _Depends: 1.1_
- [ ] 5.2 (P) 部イベントパースの単体テストを追加する
  - 日付行のパース、年跨ぎの推定、ナビゲーションフッター混在でも日付行のみ抽出、の各ケースを検証する
  - 完了状態: 3ケースそれぞれの検証が揃い、すべて成功する
  - _Requirements: 4.1_
  - _Depends: 1.4_
- [ ] 5.3 (P) パス系サーバークエリの単体テストを追加する
  - 前方一致、日本語を含むパスの正規表現エスケープ、viewer 権限で見えないページの除外、更新日時降順・件数制限、複数パス解決の入力順保持と除外、を検証する
  - 完了状態: 上記それぞれの検証が揃い、すべて成功する
  - _Requirements: 2.1, 2.2, 3.5_
  - _Depends: 1.5_
- [ ] 5.4 (P) サーバーAPIの結合テストを追加する
  - パス配下最近ページ（未ログイン拒否・viewer フィルタ）、複数パス解決（配列でないボディは拒否・自分の権限でフィルタ）、サイト共通設定更新（非管理者拒否・3キー永続化・監査アクティビティ）、個人別設定更新（永続化・他利用者に非影響）を検証する
  - 完了状態: いずれのケースも期待する応答（エラー種別・返却内容・永続化）が確認できる
  - _Requirements: 2.1, 3.3, 3.5, 5.3, 6.2_
  - _Depends: 2.1, 2.2, 2.3_
- [ ]* 5.5 (P) 新ウィジェットのコンポーネントテストを追加する
  - Classroom最新投稿・ピン留めページ・お知らせ予定フィードの各ウィジェットについて、内容ありのときと空のときの表示、ピン留め未設定時の管理者向け案内の出し分け、フィードの1セクション失敗時に他セクションが表示されること、を検証する
  - 完了状態: 各ウィジェットの内容あり・空・出し分けの検証が成功する
  - _Requirements: 2.4, 3.4, 4.5_
  - _Depends: 3.3, 3.4, 3.5_
- [ ] 5.6 ホーム画面のE2E・回帰確認を行う
  - 管理者がウィジェットの順序変更と非表示を保存 → 個人別未設定の利用者のホームに反映される
  - 利用者がホーム上でウィジェットを並べ替え・非表示にして保存 → リロード後も維持、「初期状態に戻す」でサイト共通設定に従う
  - ゲストがホームを開く → ウィジェットエリアは出ず、お知らせと動作環境の要件表のみ表示される
  - `/home` が引き続き非編集の独立ページで、サイドバーのナビアイコン・ログイン後の既定遷移先・下部のお知らせ（空欄なら管理者にのみ案内）と要件表が変わっていない
  - 完了状態: 上記シナリオがそれぞれ期待どおりに動作することを確認できる
  - _Requirements: 5.3, 6.1, 6.2, 6.4, 8.1, 8.2, 9.2, 9.3, 9.4_
  - _Depends: 4.1, 4.2, 4.3, 4.4_
