# Implementation Plan: home-page-widgets

- [ ] 1. Foundation: サーバー側の下準備
- [x] 1.1 お知らせ本文を保持する管理設定キーを追加する
  - `customize:noscript` と同型の、環境変数を持たない文字列設定として定義する
  - 既定値は未設定（空）とする
  - 完了状態: 設定管理経由でこのキーを読み書きでき、初期状態では未設定値が返ることを確認できる
  - _Requirements: 6.1_
- [x] 1.2 (P) 自分の書きかけ(WIP)ページを権限フィルタ済みで取得するサーバークエリを追加する
  - 既存のページ一覧取得サービスに、指定ユーザーが最終更新者である WIP ページのみを対象とするメソッドを追加する
  - 既存の viewer 権限フィルタ機構を再利用し、最終更新者一致だけで権限判定しない
  - 完了状態: 対象ユーザー以外の WIP ページ、閲覧権限のないページ、WIP でないページが結果に含まれないことをテストで確認できる
  - _Requirements: 4.2, 4.3_
  - _Boundary: page-listing service_

- [ ] 2. Foundation: API と移行
- [x] 2.1 自分の書きかけ(WIP)ページ一覧を返す API エンドポイントを追加する
  - ログイン済み利用者のみアクセス可能とする
  - 完了状態: 未ログイン時は認可エラー、ログイン時は自分の WIP ページ一覧のみが返ることを確認できる
  - _Requirements: 4.2_
  - _Depends: 1.2_
- [x] 2.2 (P) `/home-notice` ページ本文を新しい設定値へ一度だけ複製する移行処理を追加する
  - 設定値が既に存在する場合は複製をスキップする
  - `/home-notice` ページが存在しない、または本文が空の場合は設定値を空のまま初期化する
  - 複製後は `/home-notice` ページ自体を削除・変更しない
  - 完了状態: 「既存値あり→スキップ」「ページなし→空初期化」「ページあり→複製」の3パターンがテストで再現・確認できる
  - _Requirements: 7.1, 7.2, 7.4_
  - _Boundary: migration_
  - _Depends: 1.1_

- [ ] 3. Core: お知らせの管理画面設定化
- [x] 3.1 お知らせ設定を更新する API エンドポイントを追加する
  - 管理者のみが更新でき、更新操作は既存の監査ログ機構に記録される
  - 完了状態: 管理者以外からの更新要求が拒否され、管理者からの更新が設定値に反映されることを確認できる
  - _Requirements: 6.1, 6.2_
  - _Depends: 1.1_
- [x] 3.2 管理画面のクライアント状態にお知らせ設定の取得・更新を追加する
  - 既存のカスタマイズ設定コンテナに、お知らせ本文の状態と更新メソッドを追加する
  - 完了状態: 管理画面側からお知らせ設定の現在値取得と更新呼び出しができることを確認できる
  - _Requirements: 6.1, 6.2_
  - _Depends: 3.1_
- [x] 3.3 お知らせ設定の編集フォームを管理画面に追加する
  - 既存の同種設定（コードエディタ形式の管理項目）と同じ操作感の編集フォームとし、保存操作を提供する
  - 完了状態: 管理画面からお知らせ本文を編集・保存でき、保存後に画面上の値が更新後の内容になることを確認できる
  - _Requirements: 6.1, 6.2_
  - _Depends: 3.2_
- [x] 3.4 ホーム画面のお知らせ取得元を管理設定に切り替える
  - `/home-notice` ページ本文を読む既存ロジックを、管理設定値を読むロジックに置き換える
  - 設定値が空の場合、管理者にのみ設定を促す案内を表示し、それ以外の利用者にはお知らせセクション自体を表示しない
  - 完了状態: 設定値を更新するとホーム画面表示が更新後の内容に変わり、`/home-notice` ページ自体を編集してもホーム画面表示に影響しないことを確認できる
  - _Requirements: 6.1, 6.2, 6.3, 6.4, 7.3_
  - _Depends: 1.1_

- [ ] 4. Core: 検索・最近更新・ブックマークウィジェット
- [x] 4.1 (P) 検索ウィジェットを実装する
  - ホーム画面上部に大きな検索ボックスを表示し、入力したキーワードで既存の検索結果画面へ遷移する
  - キーワード未入力での検索実行はエラー画面に遷移させず無害に処理する
  - 完了状態: キーワードを入力して実行すると検索結果画面に遷移し、未入力での実行がエラーにならないことを確認できる
  - _Requirements: 2.1, 2.2, 2.3_
  - _Boundary: SearchWidget_
- [ ] 4.2 (P) 最近更新されたページウィジェットを実装する
  - 既存の最近更新ページ取得の仕組みを用いて、更新日時の新しい順に一定件数を表示する
  - 閲覧可能な更新済みページが無い場合は空状態のメッセージを表示する
  - 完了状態: 一覧内のページ名クリックで該当ページへ遷移でき、閲覧権限のないページが一覧に含まれないことを確認できる
  - _Requirements: 3.1, 3.2, 3.3, 3.4_
  - _Boundary: RecentUpdatesWidget_
- [ ] 4.3 (P) 自分のブックマークウィジェットを実装する
  - 既存のブックマーク取得の仕組みを用いて、利用者自身のブックマークページを一覧表示する
  - ブックマークが1件も無い場合は空状態のメッセージを表示する
  - 完了状態: 他の利用者のブックマークが一覧に含まれず、一覧内のページ名クリックで該当ページへ遷移できることを確認できる
  - _Requirements: 4.1, 4.3, 4.4, 4.5_
  - _Boundary: BookmarksWidget_

- [ ] 5. Core: 書きかけ(WIP)ページウィジェット
- [ ] 5.1 自分の書きかけ(WIP)ページを取得するクライアント側の仕組みを追加する
  - タスク2.1のエンドポイントを呼び出し、結果をキャッシュ・再利用できる形で提供する
  - 完了状態: ホーム画面のコンポーネントから自分の WIP ページ一覧を取得できることを確認できる
  - _Requirements: 4.2_
  - _Depends: 2.1_
- [ ] 5.2 書きかけ(WIP)ページウィジェットを実装する
  - タスク5.1の仕組みを用いて、利用者自身の書きかけページを一覧表示する
  - 書きかけページが1件も無い場合は空状態のメッセージを表示する
  - 完了状態: 他の利用者の書きかけページが一覧に含まれず、一覧内のページ名クリックで該当ページへ遷移できることを確認できる
  - _Requirements: 4.2, 4.3, 4.4, 4.5_
  - _Depends: 5.1_

- [ ] 6. Integration: ホーム画面への統合
- [ ] 6.1 ウィジェットエリアのコンテナを実装する
  - 4ウィジェット（検索・最近更新・ブックマーク・書きかけ）を固定順に描画し、実行時の表示切替・並び替え機能は設けない
  - 各ウィジェットを個別のエラー境界で包み、1つの取得・描画失敗が他ウィジェットやページ全体の表示に影響しないようにする
  - 完了状態: いずれか1ウィジェットの取得を意図的に失敗させても、他のウィジェットとページ全体が正常に表示され続けることを確認できる
  - _Requirements: 1.1, 1.2, 1.3, 1.4_
  - _Depends: 4.1, 4.2, 4.3, 5.2_
- [ ] 6.2 ホーム画面の表示をログイン状態で出し分ける
  - ログイン済み利用者にはウィジェットエリアを表示し、匿名ゲストにはウィジェットエリアを表示せずお知らせと動作環境の要件表のみを表示する（v1と同じ既存動作を維持）
  - 完了状態: ログイン利用者とゲストでそれぞれホーム画面を表示し、ウィジェットエリアの表示有無が要件どおりであることを確認できる
  - _Requirements: 5.1, 5.2_
  - _Depends: 6.1, 3.4_

- [ ] 7. Validation
- [ ] 7.1 書きかけ(WIP)ページ取得クエリの単体テストを追加する
  - WIP でないページ、他利用者が最終更新者のページ、閲覧権限のないページが結果から除外されることを検証する
  - 完了状態: 上記3パターンそれぞれについて除外を確認するテストが揃い、すべて成功する
  - _Requirements: 4.2, 4.3_
  - _Depends: 1.2_
- [ ] 7.2 お知らせ移行処理の単体テストを追加する
  - 既存値あり→スキップ、`/home-notice` 無し→空初期化、`/home-notice` あり→複製、の3パターンを検証する
  - 完了状態: 3パターンそれぞれの検証テストが揃い、すべて成功する
  - _Requirements: 7.1, 7.2, 7.4_
  - _Depends: 2.2_
- [ ] 7.3 API の結合テストを追加する
  - 書きかけページ一覧エンドポイントの未ログイン時エラー・ログイン時の結果、お知らせ更新エンドポイントの非管理者エラー・管理者による更新反映を検証する
  - 完了状態: いずれのケースも期待する応答（エラー種別・返却内容）が確認できる
  - _Requirements: 4.2, 6.1, 6.2_
  - _Depends: 2.1, 3.1_
- [ ] 7.4* 各ウィジェットの空状態表示のコンポーネントテストを追加する
  - 最近更新・ブックマーク・書きかけの各ウィジェットについて、対象データが0件のときの空状態メッセージ表示を検証する
  - 完了状態: 3ウィジェットそれぞれの空状態テストが成功する
  - _Requirements: 3.3, 4.4_
  - _Depends: 4.2, 4.3, 5.2_
- [ ] 7.5 ホーム画面のE2E確認を行う
  - ログイン利用者が `/home` を開き、4ウィジェットと下部のお知らせ・要件表がすべて表示されることを確認する
  - 匿名ゲストが `/home` を開き、ウィジェットエリアが表示されずお知らせ・要件表のみ表示されることを確認する
  - 完了状態: 上記2パターンの表示結果がそれぞれ期待どおりであることを確認できる
  - _Requirements: 1.1, 5.1, 5.2_
  - _Depends: 6.2_
- [ ] 7.6 v1からの継続性を回帰確認する
  - `/home` が引き続き `/nas` 型の非編集ページであること、サイドバーのナビアイコン、ログイン後の既定遷移先、動作環境の要件表の表示が変更されていないことを確認する
  - 完了状態: v1で確認されていたこれら4点の挙動が本変更後も変わっていないことを確認できる
  - _Requirements: 8.1, 8.2, 8.3, 8.4_
  - _Depends: 6.2_

## Implementation Notes
- `AdminCodeEditor` only supports `javascript`/`css`/`html` languages (no markdown highlighter) — `CustomizeHomeNoticeSetting.tsx` uses `language="html"` as the closest available option, same substitution `CustomizeNoscriptSetting.tsx` already makes for its own content. Does not block requirements 6.1/6.2; a follow-up could add a `markdown` entry to `AdminCodeEditor`'s language map for better UX.
- Admin customize settings' i18n keys (e.g. `customize_settings.*`) actually live in `admin.json`, not `translation.json` as design.md loosely stated.
- Non-admin/unauthenticated apiv3 GET routes in this codebase return 403 (not 401), and admin-gated PUT routes redirect (302, not 403) for non-admins — both are pre-existing shared middleware behavior (`loginRequiredFactory`, `adminRequiredFactory`), not defects introduced by this feature. design.md's API contract tables list 401/403 respectively; treat those as documentation-only imprecision.

- The migration file (`20260905010000-migrate-home-notice-to-config.js`) hardcodes its own local `/home-notice` path literal instead of importing `HOME_NOTICE_PATH` from `features/home/consts.ts` — minor SSOT drift, non-blocking, optional cleanup later.
