export { HomeContent } from './client/components/HomeContent';
export { HomeNavItem } from './client/nav/HomeNavItem';
export { HOME_NOTICE_PATH } from './consts';
// Server-only: import `getHomeNoticeMarkdown` from './server/get-home-notice'
// directly (it pulls the page service into whatever imports it).
